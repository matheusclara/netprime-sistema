services:
  netprime:
    build: .
    container_name: netprime_sistema
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:5000"
    environment:
      TZ: America/Sao_Paulo
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./backups:/app/backups
